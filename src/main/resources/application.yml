server:
  port: 8081

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/files_upload_service?useSSL=false
    username: postgres
    password: BOBUR2255
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update

  servlet:
    multipart:
      file-size-threshold: 2KB
      max-file-size: 20MB
      max-request-size: 30MB